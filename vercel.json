{
    "version": 2,
    "builds": [
        {
            "src": "config/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "config/server.js"
        }
    ]
}